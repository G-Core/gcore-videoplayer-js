It requires Node.js. Other packages are managed via npm.

Use npm to install the project root dependencies.

Then descend to the ./packages/player or other package of interest and install its dependencies too.

Run tests to ensure that the local setup is nice and clean.
