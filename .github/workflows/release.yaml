name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  npm:
    runs-on: ubuntu-latest
    steps:
      - uses: ./npm.yaml
  cdn:
    runs-on: ubuntu-latest
    needs: npm
    steps:
      - uses: ./publish.yaml
        with:
          version: ${{ github.ref_name }}
          latest: true
